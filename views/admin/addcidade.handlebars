{{#each erros}}
    <div class="alert alert-danger">{{texto}}</div>
{{else}}

{{/each}}
<h1>Cidades</h1>
<div class="card">
    <div class="card-body">
        <form action="novo" method="POST">
                <label for="nome">Nome: </label> 
                <input type="text" id="nome" name="nome" placeholder="Nome" class="form-control">
                <input type="hidden" name="usuarioCadastro" value="{{nomeDoUsuario}}">
                <br>
                <button type="submit" class="btn btn-success" >Criar Cidade</button>
        </form>
    </div>
</div>